<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bank.mapper.CustomerMapper">

<!--<select id="mySelectPage" resultType="Customer">-->
<!--        select * from  (select a.*,concat(concat(b.name,'-'),b.employee_id) as 'uploadUserName' from  customer a,user b-->
<!--                  where a.upload_user_id = b.id ) as c-->
<!--        ${ew.customSqlSegment}-->
<!--</select>-->
</mapper>

